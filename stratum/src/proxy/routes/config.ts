/**
 * Configuration API endpoints.
 *
 * GET   /v1/config → Returns org-level CQ configuration
 * PATCH /v1/config → Update org config (triggers eval re-run)
 */

// TODO: Implement config routes (Phase 2)
