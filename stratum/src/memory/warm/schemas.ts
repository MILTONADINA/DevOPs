/**
 * Zod Schemas for Structured Fact Types
 *
 * All fact extraction outputs are validated against these schemas
 * before writing to Supabase. Invalid data is discarded.
 *
 * Fact types: FunctionChange, TechDecision, PolicyUpdate, Todo, VariableChange
 */

// TODO: Implement Zod schemas (Phase 3)
