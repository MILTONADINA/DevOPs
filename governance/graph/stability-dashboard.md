# Graph stability dashboard

Updated per cycle. This is the go/no-go instrumentation for Phase 1 → Phase
2 (see `autonomy-config.yml`'s `phase_2_entry_criteria`). Per the 2026 DORA
research this project's design leans on: **anchor on stability, not
throughput** — a rising cycle count with a worsening change-failure-rate is
a regression, not progress, and must not be read as "the graph is working."

## Current state (2026-09-14)

Phase 0 started. 5 cycles run so far. This table gets a row per completed
sprint-cycle.

| Cycle | Date | Backlog item | Outcome | Deploy/billing gate hit? | Claim-validator result | Change-failure? | Environment faults | Notes |
|---|---|---|---|---|---|---|---|---|
| `phase0-001-lockfile` | 2026-09-14 | SHIP_BLOCKERS.md 1.7 — no lockfile at DevOps-core root | Ready-for-PR per validator; commit `6bc60be` landed (653-line lockfile only, clean, correctly scoped) | Not triggered (not a deploy/billing path) | Not re-run this cycle | No | 0; n/a | **Process deviation, not a technical failure**: the pipeline committed autonomously with no human checkpoint, violating the Phase 0 rule. Root-caused and fixed (prompt-level hard constraint on planner+coder) — see `role-mapping.md`'s "Known limitation" section for the fix and the still-open follow-up (worktree isolation for a real deterministic gate). |
| `phase0-002-ci-fixes` | 2026-09-14 | SHIP_BLOCKERS.md 1.2 — 3 CI gates red 7+ days (DeepTeam install, Sigstore trust-bundle, gitleaks false positive) | **Not ready-for-PR on first pass — correctly.** Pipeline found a real, self-introduced defect (unanchored gitleaks allowlist regex, `governance/gitleaks-ci.toml`) via tester+reviewer+security all independently reproducing it with decoy files, and correctly refused to sign off. Commit-prevention fix from cycle 1 held — nothing was auto-committed. Also independently, separately: an external automated security review caught a HIGH supply-chain issue in the Sigstore fix (unverified fetch of a mutable trust root) mid-cycle; fixed manually (`cosign initialize`) before the pipeline's own security stage reached that file, so this cycle didn't get to prove whether its security stage would have caught *that* one independently. The unanchored-regex defect it *did* catch on its own is a strong, real signal the quality gates work. | Not triggered | Not re-run this cycle | No (caught before merge) | 0; n/a | Human (me) independently re-verified the anchor fix with the real gitleaks binary using repo-relative paths (the pipeline's own decoy methodology, redone from scratch rather than trusted) before committing. All three fixes then committed together as one human-reviewed change. |
| `phase0-003-npm-audit` | 2026-09-14 | SHIP_BLOCKERS.md 1.5 — Stratum dependency vulnerabilities | **Not ready-for-PR on first pass — correctly, on a minor issue.** Real engineering (26→14 vulns via non-force `npm audit fix`, no silent `--force`) was sound and independently re-verified by security/validator/human. Blocker was a self-contradicting number in an internal scratch artifact (`npm-audit-baseline-summary.md`: table said 16/8/2, prose said 19/5) that no later task corrected — tester caught it, reviewer/validator correctly refused sign-off over an unresolved tester failure. Fixed the artifact, committed the real dependency fix (`1fd0d46`) + a SHIP_BLOCKERS.md update (`3d088fd`) after independently re-verifying with the real gitleaks/npm audit tools myself. | Not triggered | Not re-run this cycle | No (caught before merge) | 0; n/a | 3 critical vulns remain, deliberately not force-fixed — needs a human call on accepting major-version bumps (vitest 5.0.0, supabase 2.117.0). |
| `phase0-004-claim-validator-investigation` | 2026-09-14 | SHIP_BLOCKERS.md 1.1 — claim-validator 97% broken, investigation-only scope | **Not ready-for-PR — for good reason, and this cycle earned its keep regardless.** Reviewer/security/validator all independently refused sign-off over: 2 unremediated tester failures (an unsourced "~57" figure; a vacuous scope-verification gate that could never detect a violation because `.workflow/proofs/` is gitignored), a real scope breach (this cycle wrote 3 new claim artifacts into the directory it was told not to touch), and a material omission (the deliverable asked the owner to choose between two invasive options while its own evidence had found a third, dominant, non-invasive one). **The investigation finding itself was correct and highly valuable**: independently reproduced by a human — all 52 distinct "missing" commit SHAs are recoverable via `git fetch origin <sha>` (GitHub retains squash-merged PRs' original commits by SHA even after the source branch is deleted; a plain `git fetch` just never follows non-ref-reachable SHAs). Recovered all 52, anchored them under `refs/claim-provenance/<sha>` (durable against `git gc`), wrote `scripts/recover-claim-provenance.sh` so this is reproducible elsewhere. **Result: validate:claims went from 3/97 to 87/120 passing** — zero claim YAML rewritten, zero validator logic changed. **Separately, this same cycle's security stage found a real HIGH-severity bypass in `deploy-gate.sh`/`block-sealed-refs.sh`** (start-anchored regex + a settings.json wrapper that only read the first line of multi-line commands defeated all three gates for realistic command shapes like `cd x && vercel deploy --prod`) — reproduced independently by a human, fixed, and re-verified against every bypass found plus the legitimate paths. See SHIP_BLOCKERS.md 1.1 and 1.8. | Not triggered (this cycle touched no deploy/billing paths itself) | **3→87 of 120 passing** (the headline result) | No (all findings caught before anything committed) | 0; n/a | Cleaned up the 3 errant claim artifacts this cycle wrote. Recovered refs are local-only — not yet pushed to origin (`--push` flag exists, left as an explicit human decision). ~33 claims still fail for unrelated reasons (missing files_changed, real re-run drift, and new-claim spec_ref format issues from this session's own graph cycles) — smaller, separate follow-up. |
| `phase0-005-test-suites` | 2026-09-14 | SHIP_BLOCKERS.md 1.3 — neither test suite runs | **Substantive work sound; validator correctly refused sign-off on hygiene; remediated by the human before landing.** (a) DevOps-core: dangling `test:skills` removed, eval registry documented honestly as a spec with no runner (a string-match stub would have been a fake test). (b) Stratum: `npm test` genuinely runs — 101 files, 825 pass, 5 todo — after a single `chmod +x`; root cause was a Windows-origin `node_modules` (no Unix exec bits), and the missing rollup native module had already been restored by the 1.5 cycle. No dependency/lockfile change. (c) CI integration still open. Validator blocked on: pre-existing redundancy-audit hunks mixed into the same files (resolved by splitting the audit into its own commit `6920f34`, cycle work isolated in `264db9c`); two claim artifacts that could never validate (replaced by re-runnable proofs 030/031); a false PB-51 sentence (reworded); proofs pinned to a stale HEAD (re-pinned to the work commit with reachability checks); a stale blocker (closed). | Not triggered | **97/126 passing** (was 87/120; all 7 of this cycle's claims validate, including under `--silent`); **97/130 after the third pass on 2026-09-15** under the full re-running validator (104/130 with `--no-rerun`; the gap is seven legacy `claim-2026-05-22-*` proofs whose re-run exits 1, pre-existing drift; the 4 newest failures are cycle 6's uncommitted claims 032–035); **122/132 full / 126/132 `--no-rerun` after the 2026-09-15 triage** — all 22 cycle 1–4 claims validate; the ten left are cycle 6's six uncommitted claims, the clean-tree and validate-claims meta-checks they drag along, and the two human items (1.6 re-sign, 1.9 visibility) — see SHIP_BLOCKERS 1.1 "Outcome of the triage" | No | 0; n/a | Two reusable process defects found and fixed: (1) proofs silently depended on the caller's npm verbosity — `npm run validate:claims --silent` leaks `npm_config_loglevel=silent` into nested npm calls and blanks the banners proofs grep for; all five proofs now unset it. (2) The tester/security prompts never stated the claim schema, so cycles 1–4 wrote ~10 unvalidatable claims — `sprint-cycle.js` prompts now carry the rules. Also caught in my own new security proofs before independent review: `cmd | tail; $?` captured tail's status, not gitleaks' — a leak could never have failed the proof. **Second and third adversarial passes (2026-09-14/15)**: the 7 proofs passed but were fragile — they hard-asserted live whole-file counts (polish backlog line/heading counts, "exactly 4" registry entries) and gitignored `node_modules` fingerprints that the ordinary next action (filing PB-52, a 5th eval, `npm ci`) would flip; the security proofs' regex tier missed GOCSPX-/sk-proj-/generic `password=` shapes; three claim descriptions carried stale check counts. Remediated: exact-set checks now read the commit (or an archived, sha-pinned snapshot for the gitignored backlog), live checks are durable invariants only, node_modules/suite checks branch on fact-keyed guards that verify the archived claim-time record in the changed state; the regex tier grew to 27 self-tested vendor shapes plus a generic-assignment tier, and `governance/gitleaks-ci.toml` gained matching custom rules (claims 012/013 restated). Every guard branch and six negative cases were exercised in an isolated worktree with a real `npm ci` — which also produced the PB-51 evidence now in SHIP_BLOCKERS 1.3. **Third pass (2026-09-15, four lenses + synthesis, `sound_to_commit: false` with four must-fix items)** found real gaps in my own remediation: the hook scripts' `+x` bit had never been committed (hidden by `core.fileMode=false`; a fresh checkout would fail closed on every command), the generic-tier placeholder filter was line-scoped (a real key prefixed `example-` was dropped), connection-URI passwords were invisible to every tier, claim 013's T1–T3 asserted nothing, and 028/029 still carried exact-mtime checks on gitignored files. Fixed in `027c159` (mode bits committed, wrapper invokes hooks via `bash` with an explicit empty-root check and visible fallback, value-scoped entropy-aware filter with the bypass as a positive control, a 26th vendor/URI alternative + custom CI rule, per-alternative branch proofs with pinned control counts, 013 with real assertions and a path-free `test_command`, mtime checks informational) — and a **fourth pass** on that commit then found more in the new controls themselves: the unquoted tier could not match a bare `TOKEN=`/`PASSWORD=` name, the placeholder filter's absolute 24-char bar dropped ordinary-length credentials containing `-test-`, the uniqueness proofs could not see a *narrowed* regex, the CI URI rule was case-sensitive, and CLAUDE.md still said hooks were not wired. Fixed in `4d7c76e` and the commit after it (bare-name matching, a relative-retention rule, six generality controls, `(?i)`, docs). The validator figure is stated with its mode because the two modes differ by seven legacy re-run failures. See SHIP_BLOCKERS 1.8 and claims 013/030/031. |
| `phase1-006-graph-dashboard` | 2026-09-14 → 2026-09-15 | Read-only localhost live dashboard for the pipeline (user: "i need a local host to see graph live updates") — `scripts/graph-dashboard/{server.mjs,index.html,README.md}`, `npm run graph:dashboard`, 4081, SSE | **Delivered and landed in `30d59a1` after the validator correctly refused sign-off; remediated by the orchestrator before landing.** 13 tasks, all tested (277 tests, 276 pass, 0 fail, 1 pre-existing skip); reviewer approved with two non-blocking violations; security passed with one TP-warning. The validator refused because (1) the backlog's literal "zero console errors" bullet had not passed — every browser load logged a `/favicon.ico` 404 (PB-56); (2) a `.playwright-mcp/` leftover the T12 coder and tester both claimed to have removed was still on disk; (3) re-running T1's own proof reproducibly gave PASS=43/FAIL=1 (its "only two files" check predated T9's README). Fixed before landing: inline `data:` favicon with the tests pinned to it (browser re-verified by the orchestrator at 1280/800 px: 0 errors, 0 warnings), leftover removed, proof refiled; and the security stage's Host-header finding (DNS rebinding against a loopback no-auth server) fixed rather than backlogged — 403 before routing for any Host but `127.0.0.1`/`localhost`, 32 new tests. | Not triggered | **`--no-rerun` 139/139** (every claim, incl. this cycle's 13 re-pinned to `30d59a1`); **full re-run at landing 136/139**: 013 and 076 are the two human items (1.9 visibility, 1.6 re-sign) and 032 was T1's proof comparing `HEAD:package.json` to the live file, which inverted the moment the work was committed — re-pinned to the carrying commit vs its parent with a reachability check, negative branch exercised; **full re-run after the re-pin: 137/139** — only the two human items remain (SHIP_BLOCKERS 1.1) | No | 3: api 2, environment 1; resume time unrecorded | **Three environment faults, none a project bug** (the DORA-style split this column will formalise in cycle 7): an API account session limit killed 10 tasks (first run), an API outage stalled one tester (second run), and at 07:30 an App Store Xcode update made every `git` command refuse to run mid-cycle (third run; PB-55 — `~/.local/bin/git` shim to the Command Line Tools git, human still needs `sudo xcodebuild -license accept`). Each needed the orchestrator: resume arguments hand-assembled twice, `priorCoderResults` + STALL RULES added to `sprint-cycle.js`, the shim. The cycle-6 coder adapted to the dead git correctly (diagnosed toolchain, scratchpad harness, kept going, never faked the suite) — but every one of the six dashboard tests resolves the repo root via a `git` subprocess, so the suite itself died with the toolchain. → `specs/graph/R-resilience.md` (cycle 7): preflight, fault classification, blocked record, mechanical resume, hermetic tests. Also found on the real data during the pre-landing browser check: a run the orchestrator killed shows as *active* with a *running* node forever (PB-57, fixable once cycle 7's run record exists). Proof theater caught, again, by recomputing rather than trusting: the validator re-ran T1's proof instead of reading its reported exit code. |

## Baseline (pre-graph), for comparison

From `SHIP_BLOCKERS.md`, as of 2026-09-14:
- Claim-validator pass rate: **3/97 (3%)** — the number Phase 0 exists to
  fix, and the number every subsequent cycle should be compared against.
- CI status: **red for 7+ consecutive days** on `main` (DeepTeam install
  failure, Sigstore trust-bundle 404).
- Test suites: **both non-functional** (DevOps-core has no test runner;
  Stratum's vitest binary lacks +x and hits a missing native module).
- Dependency vulnerabilities (Stratum): **1 critical, 8 high, 2 moderate**.

Phase 1 autonomy should not be considered until this baseline is
demonstrably fixed and stable — not merely "fixed once."
