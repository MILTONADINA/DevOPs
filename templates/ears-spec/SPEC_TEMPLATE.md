# <Feature Name>

**Spec ID**: <domain>/<feature>  (matches file path under `/specs/`)
**Status**: draft | approved | implemented | deprecated
**Last updated**: YYYY-MM-DD
**Owner**: <user>
**Reviewers**: <users>

---

## Context

<2-4 sentences. Why does this exist? What problem does it solve? Who asked for it?>

## Out of scope

<what this feature is NOT doing — keeps reviewers anchored>

- ...
- ...

## Actors and data

- **Primary actors**: <e.g., end user, admin, system, agent>
- **Data classes touched**:
  - [ ] none
  - [ ] PII (email, name, address)
  - [ ] PHI (health-protected)
  - [ ] PCI (cardholder data)
  - [ ] children's data (COPPA scope)
  - [ ] financial
  - [ ] credentials / secrets
- **Compliance scope**:
  - [ ] none
  - [ ] COPPA
  - [ ] GDPR
  - [ ] HIPAA
  - [ ] SOC 2
  - [ ] PCI DSS
  - [ ] other: ___________

---

## Functional requirements (EARS)

> Use only EARS patterns. See `docs/EARS_GUIDE.md`.

### REQ-1 (Ubiquitous)
THE SYSTEM SHALL <action>.

### REQ-2 (Event-driven)
WHEN <trigger>, THE SYSTEM SHALL <action>.

### REQ-3 (State-driven)
WHILE <state>, THE SYSTEM SHALL <action>.

### REQ-4 (Unwanted behavior)
IF <undesirable condition>, THEN THE SYSTEM SHALL <action>.

### REQ-5 (Optional feature)
WHERE <feature is present>, THE SYSTEM SHALL <action>.

---

## Acceptance criteria

> Each AC is a testable assertion mapping to one or more REQs.

### AC-1 (maps to REQ-1)

**Given** <state>
**When** <action>
**Then** <outcome>

### AC-2 (maps to REQ-2)

...

---

## Non-functional requirements

### NFR-1 — Performance
- p95 latency: < ___ ms
- Throughput: ≥ ___ req/s
- Memory: ≤ ___ MB at p95

### NFR-2 — Observability
- All endpoints emit OTel spans with `user_id` and `tenant_id` in baggage
- PII is redacted at the span exporter (see `observability/pii-redaction.ts`)
- Errors logged at WARN+ with stack trace; INFO for normal events

### NFR-3 — Security
- Threat model: `docs/threat-models/<feature>.md` (STRIDE + OWASP ASI 2026)
- Authentication required: yes / no
- Authorization model: <role | scope | ABAC | none>
- Input validation: <schema reference>
- Rate limiting: <limit / scope>

### NFR-4 — Accessibility
- WCAG 2.2 AA compliance (axe-core scan must pass)
- Keyboard navigable
- Screen reader friendly (semantic HTML, ARIA labels)

### NFR-5 — Compliance (if applicable)
- Audit trail: every state change logged with `user_id`, timestamp, before/after
- Retention: <duration>
- Right to deletion: parent-initiated for under-13 in COPPA scope, etc.

---

## Threat model

See `docs/threat-models/<feature>.md`. Summary of mitigations:

- ASI01 (Goal Hijacking): <how mitigated>
- ASI04 (Indirect Injection): <how mitigated>
- STRIDE: <summary>

---

## Decisions

Non-obvious choices are recorded in ADRs:

- ADR-NNNN: <decision> — `docs/decisions/ADR-NNNN-<slug>.md`

---

## Open questions

> Resolve all before status → approved.

1. ...
2. ...

---

## Implementation plan

See `.workflow/state/plans/<spec-id>.md` (generated by `plan-decomposition`).

## Test plan

> Generated from ACs. Each AC should have at least one test.

- AC-1 → `tests/<path>/<file>.test.ts::<name>`
- AC-2 → ...

---

## Change log

- YYYY-MM-DD <user>: created
- YYYY-MM-DD <user>: approved AC-1 through AC-5
- YYYY-MM-DD <user>: added NFR-5 after compliance review
