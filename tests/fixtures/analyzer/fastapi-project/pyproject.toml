[project]
name = "fastapi-fixture-project"
version = "0.0.0"
description = "Analyzer fixture for REQ-D4 / AC-D4.1: a minimal FastAPI project that should trigger stack-specific/fastapi/dependency-injection in recommended.skills."
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn>=0.32.0",
]
